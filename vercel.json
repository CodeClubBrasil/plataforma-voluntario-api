{
    "version": 2,
    "builds": [
      { "src": "npm run build", "use": "@vercel/static-build" },
      { "src": "/", "use": "@vercel/node" }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "dist/main.js",
        "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"]
      }
    ]
  }
  