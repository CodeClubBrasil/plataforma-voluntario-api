{
    "version": 4,
    "builds": [
      {
        "src": "src/main.ts",
        "use": "@vercel/node",
        "config": { "includeFiles": ["**/*.ts"] }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "src/main.ts" }
    ],
    "env": {
      "NODE_ENV": "production"
    }
  }
  